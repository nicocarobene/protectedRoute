export function DetailPage(){
    return(
        <div>
            <h1 className="font-bold text-center mt-10 mb-10">Detail Page</h1>
            <h2>(Protected: authenticated user with role 'admin' required)</h2>
        </div>
    )
}